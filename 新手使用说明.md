# LYX 作品网站使用说明

## 第一次上线

1. 注册或登录 GitHub，新建一个空仓库（例如 `lyx-portfolio`）。
2. 在本文件夹空白处按住 Shift 点右键，选择“在此处打开 PowerShell”，依次运行下面两行（把网址换成你的仓库网址）：

   `git remote set-url origin https://github.com/你的用户名/lyx-portfolio.git`

   `git push -u origin main`

   如果 GitHub 仓库默认分支是 master，则把上一行的 `main` 换成 `master`。
3. 登录 Vercel，选择 **Add New → Project**，导入你的 GitHub 仓库。
4. Vercel 会自动识别 Next.js，直接点击 **Deploy**。不需要购买服务器。
5. 发布成功后，Vercel 会给你一个免费的 `xxx.vercel.app` 地址。以后也可以绑定自己的域名。

## 日常上传作品（不用改代码）

1. 双击 **打开作品管理器.bat**。
2. 浏览器会打开作品管理页面，点击“新增作品”或“编辑”。
3. 填文字、选择封面和详情图片/MP4，点击“保存作品”。
4. 想先检查效果，可双击 **本地预览网站.bat**。
5. 确认后双击 **一键发布网站.bat**。Vercel 通常会在 1–3 分钟内自动更新。

## 图片建议

- 封面：横图，建议 1920×1080，WebP 或 JPG。
- 详情图：WebP/JPG/PNG；大图建议宽度 1600px 以上。
- 视频：仅 MP4，建议压缩到 20MB 内，避免网页加载缓慢。
- 不要使用带空格和中文的“英文网址名”，例如 `brand-redesign`。

## 注意

- 原项目许可证要求保留原设计与开发者 Evangelos Giatsidis / giats.me 的署名。
- 第一次上线需要你本人登录 GitHub 和 Vercel完成授权；之后日常发布只需双击脚本。
- 作品原图和视频会进入 `public/作品网址名/` 文件夹，请同时做好本地备份。
